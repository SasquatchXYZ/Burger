// Import Express & the Model
const express = require('express');
const router = express.Router();

const burger = require('../models/burger');

// Export routes for server.js to use.
module.exports = router;